# Scripts

This folder is intended for helper scripts and utilities that are not part of the main frontend/backend app.

Move tools such as PDF extraction helpers or one-off utilities into this folder so the application structure remains focused.
